# JVM jargon generator

A toy project written in Kotlin and Spring Boot to generate _very valuable_ jargon for you.

To run:

```
git@github.com:theothermattm/kotlin-jvm-jargon-generator.gitgradle bootRun
```
